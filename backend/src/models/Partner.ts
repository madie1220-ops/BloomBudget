import mongoose, { Schema, Document, Types } from 'mongoose';\n\nexport interface IPartner extends Document {\n  userId: Types.ObjectId;\n  partnerId: Types.ObjectId;\n  status: 'pending' | 'accepted' | 'rejected' | 'blocked';\n  sharedBudgets: Types.ObjectId[];\n  sharedTransactions: boolean;\n  createdAt: Date;\n  updatedAt: Date;\n}\n\nconst partnerSchema = new Schema<IPartner>(\n  {\n    userId: {\n      type: Schema.Types.ObjectId,\n      ref: 'User',\n      required: true\n    },\n    partnerId: {\n      type: Schema.Types.ObjectId,\n      ref: 'User',\n      required: true\n    },\n    status: {\n      type: String,\n      enum: ['pending', 'accepted', 'rejected', 'blocked'],\n      default: 'pending'\n    },\n    sharedBudgets: [\n      {\n        type: Schema.Types.ObjectId,\n        ref: 'Budget'\n      }\n    ],\n    sharedTransactions: {\n      type: Boolean,\n      default: true\n    }\n  },\n  { timestamps: true }\n);\n\n// Ensure unique partnership (no duplicate invites)\npartnerSchema.index({ userId: 1, partnerId: 1 }, { unique: true });\n\nexport default mongoose.model<IPartner>('Partner', partnerSchema);"
