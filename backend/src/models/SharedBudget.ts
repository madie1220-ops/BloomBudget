import mongoose, { Schema, Document, Types } from 'mongoose';\n\nexport interface ISharedBudget extends Document {\n  budgetId: Types.ObjectId;\n  ownerId: Types.ObjectId;\n  sharedWithUserId: Types.ObjectId;\n  permissions: 'view' | 'edit' | 'full';\n  createdAt: Date;\n  updatedAt: Date;\n}\n\nconst sharedBudgetSchema = new Schema<ISharedBudget>(\n  {\n    budgetId: {\n      type: Schema.Types.ObjectId,\n      ref: 'Budget',\n      required: true\n    },\n    ownerId: {\n      type: Schema.Types.ObjectId,\n      ref: 'User',\n      required: true\n    },\n    sharedWithUserId: {\n      type: Schema.Types.ObjectId,\n      ref: 'User',\n      required: true\n    },\n    permissions: {\n      type: String,\n      enum: ['view', 'edit', 'full'],\n      default: 'view'\n    }\n  },\n  { timestamps: true }\n);\n\n// Ensure unique sharing (no duplicate shares)\nsharedBudgetSchema.index({ budgetId: 1, sharedWithUserId: 1 }, { unique: true });\n\nexport default mongoose.model<ISharedBudget>('SharedBudget', sharedBudgetSchema);"
